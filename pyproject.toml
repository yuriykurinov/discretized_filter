[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "discretized-filter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "numba",
    "scipy",
    "matplotlib",
]

[tool.setuptools.packages.find]
where = ["src"]
